import React from 'react';
import UserInfo from './UserInfo';

import './index.scss';

const Header = () => (
  <div>
    <header className="Header">
      <UserInfo />
    </header>
  </div>
);

export default Header;
